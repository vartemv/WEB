import { Device } from "@/types";
import { FunctionComponent } from "react"

interface DeviceDetails {
    selDevice: Device;
}

const Details: FunctionComponent<DeviceDetails> = ({selDevice}) => {
    return(
        <></>
    )
};

export default Details;